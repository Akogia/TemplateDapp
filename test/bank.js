//const { assert } = require('chai');
//
//const Bank = artifacts.require('Bank');
// 
//
//function token(n) {
//    return web3.utils.toWei(n, 'ether')
//}

contract('Test new Bank', (accounts) => {
  //it('should store an information', async () => {
  //  const BankInstance = await Bank.new();
  //  // Set information "RSK"
  //  await BankInstance.setInfo("RSK", { from: accounts[0] });
  //  // Get information value
  //  const storedData = await BankInstance.getInfo();
  //  assert.equal(storedData, "RSK", 'The information RSK was not stored.');
  //});

    //describe('has been deployed', async ()=> {
    //    it('should deposit hopefully', async () => {
    //        const BankInstance = await Bank.new()
    //        
    //    });
    //})


});